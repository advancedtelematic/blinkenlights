from setuptools import setup

setup(
   name='blinkenlights',
   version='0.0.1',
   description='Some lights doing stuff',
   author='OTA',
   author_email='OTA@example.com',
   packages=['blinkenlights'],  #same as name
   # install_requires=['bar', 'greek'], #external packages as dependencies
)
